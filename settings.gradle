pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = "artifactregistry://us-west1-maven.pkg.dev/moderne-dev/moderne-releases" }
    }
}

buildscript {
    repositories {
        gradlePluginPortal()
    }
    dependencies {
        classpath("gradle.plugin.com.google.cloud.artifactregistry:artifactregistry-gradle-plugin:latest.release")
    }
}
apply plugin: "com.google.cloud.artifactregistry.gradle-plugin"
rootProject.name = "moderne-organizations"
